import Button from "./Button";

export default function InsightSecondaryButton() {
    const onClickHandler = () => {};

    return <Button className="insight-secondary-btn" title="Disable Ad Blocker" onClick={onClickHandler} />;
}
