export interface QuestDefinitionDTO {
    id: number;
    title: string;
    description: string;
    requirementType: string;
    requirement: number;
    rewardType: string;
    rewardAmount: number;
}
